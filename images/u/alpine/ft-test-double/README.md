# File Transfer Servers Test Double

This server is intended as a test double for FTP related tests.

The user is expected to prepare the necessary ssh keys and ssl certificates and mount their folders, while declaring the actual file names in the proftpd configuration file.

See `test/01-ft-example` folder for an example.
