# IWHI Edge debug enabled image

If built from the sandbox, pull the base image upfront before launching the sandbox, as the sandbox does not inherit the logins from the host.

```sh
docker pull iwhicr.azurecr.io/webmethods-edge-runtime:12.0.5.0
```
