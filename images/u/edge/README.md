# IWHI Edge Based Images

If images in this folder are built from the sandbox, pull the base image upfront before launching the sandbox, as the sandbox does not inherit the logins from the host.

Example:

```sh
docker pull iwhicr.azurecr.io/webmethods-edge-runtime:12.0.6.0
```
