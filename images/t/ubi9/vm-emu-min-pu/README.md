# Minimal UBI9 VM Emulator Containing Framework's POSIX Utils

This container adds all the available "posix utils" from the sibling repository in the framework in a minimal UBI89 based virtual machine emulator.
