# Minimal UBI9 VM Emulator Containing Framework's POSIX Utils and WMUI

This container adds all the available "posix utils" from the sibling repository in the framework in a minimal UBI9 based virtual machine emulator, plus the webMethods Unattended Installations functions.

